(jbuild_version 1)

(executable
  ((name combinators)                 ; The name of your entry file, minus the .ml
   (public_name Bus) ; Whatever you like, as far as I can tell
   (libraries (lib))))         ; Express a dependency on the "lib" module
