(jbuild_version 1)

(executable
  ((name example)                 ; The name of your entry file, minus the .ml
   (public_name Bus) ; Whatever you like, as far as I can tell
   (libraries (bus))))         ; Express a dependency on the "lib" module
